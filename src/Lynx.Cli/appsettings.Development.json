{
  "NLog": {
    "targets": {
      "console": {
        "type": "ColoredConsole",
        "layout": "${date:format=HH\\:mm\\:ss} [${uppercase:${level}}] | ${message} ${exception:format=tostring}"
      }
    }
  }
}
